#content
  font-size 30px
.bg
  width 100%
  height  0
  padding-top: (300 / 640) * 100 + '%';
  background url('images/bg.png') left top no-repeat
  background-size 100% auto

.image
  width 100%
  align-items center
  img
    width 100%
